/** @jsx React.DOM */

'use strict';

var React = require('react'),

    Header = React.createClass({
      render: function() {
        return (
          <h1 className="Header">RMD</h1>
        );
      }
    });

module.exports = Header;
